{
  "name": "@settlement/integration-cctp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CCTP V2 client interface + in-memory mock (no network code yet)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@settlement/config": "workspace:*"
  }
}
